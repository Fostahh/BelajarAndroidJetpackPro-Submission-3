<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mohammadazri.bajp3rddicodingsubmission">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BAJP2ndDicodingSubmission">
        <activity android:name=".ui.activities.FavoriteActivity" />
        <activity android:name=".ui.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.DetailTvShowActivity"
            android:label="@string/title_detail_tvshow"
            android:parentActivityName=".ui.activities.MainActivity" />
        <activity
            android:name=".ui.activities.DetailMovieActivity"
            android:label="@string/title_detail_movie" />
        <activity
            android:name=".ui.activities.MainActivity"
            android:label="@string/title_main_activity" />
    </application>

</manifest>